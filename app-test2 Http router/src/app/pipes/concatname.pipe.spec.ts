import { ConcatnamePipe } from './concatname.pipe';

describe('ConcatnamePipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatnamePipe();
    expect(pipe).toBeTruthy();
  });
});
